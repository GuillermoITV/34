# Instalar el módulo ImportExcel (puede requerir permisos de administrador)
# Install-Module -Name ImportExcel -Force -Scope CurrentUser

# Importar el módulo ImportExcel
Import-Module ImportExcel

# Ruta al archivo Excel
$rutaArchivoExcel = "C:\Ruta\Al\Archivo.xlsx"

# Obtener el valor de la última celda en la columna G
$valorUltimaCeldaG = (Import-Excel -Path $rutaArchivoExcel -WorksheetName "NombreDeLaHoja" | 
                      Sort-Object G -Descending | 
                      Select-Object -First 1 -Property G).G

# Imprimir el valor de la última celda en la columna G
Write-Host "El valor de la última celda en la columna G es: $valorUltimaCeldaG"


